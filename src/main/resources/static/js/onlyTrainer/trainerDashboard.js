// function truncateText() {
//     const lessonElements = document.querySelectorAll('.dsh-class-content');
//
//     lessonElements.forEach(lesson => {
//         const titleElement = lesson.querySelector('.dsh-lesson-text');
//         const originalText = titleElement.textContent;
//         const maxLength = 289; // 원하는 최대 길이
//
//         if (originalText.length > maxLength) {
//             const truncatedText = originalText.substring(0, maxLength) + '...';
//             titleElement.textContent = truncatedText;
//         }
//     });
// }
//
// window.addEventListener('DOMContentLoaded', truncateText);
